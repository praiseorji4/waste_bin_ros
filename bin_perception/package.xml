<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>bin_perception</name>
  <version>0.0.1</version>
  <description>YOLO-based object detection node for the bin robot.</description>
  <maintainer email="praiseorji4@gmail.com">chibueze</maintainer>
  <license>Apache-2.0</license>

  <depend>rclpy</depend>
  <depend>sensor_msgs</depend>
  <depend>cv_bridge</depend>

  <depend>std_msgs</depend>
  <exec_depend>python3-opencv</exec_depend>
  <!-- peace_detector needs mediapipe, which has no rosdep key.
       Install it with pip; see the docstring in peace_detector.py for the exact
       commands (note: XML comments cannot contain double hyphens, so the pip
       flags are not repeated here). -->
  <exec_depend>python3-numpy</exec_depend>

  <buildtool_depend>ament_python</buildtool_depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
